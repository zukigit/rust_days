Day 6 Exercises
1. Create a Circle struct with a radius field. Add methods for area() and circumference(). Add a new() constructor.
2. Create a BankAccount struct with owner: String and balance: f64. Add methods deposit(), withdraw() (don't allow going below zero), and print_statement().
3. Add #[derive(Debug)] to your structs and print them with {:#?}.